let numeros = [];

numeros[0] = prompt();
numeros[1] = prompt();
numeros[2] = prompt();
numeros[3] = prompt();
numeros[4] = 90;

for (let x =0; x < numeros.length; x++) {
    console.log(numeros[x]);
}

console.log(numeros.length);